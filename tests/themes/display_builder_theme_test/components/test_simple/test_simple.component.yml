name: Test simple
description: "Used for testing Display Builder."
links:
  - "https://www.drupal.org/project/display_builder"
slots:
  slot_1:
    title: Slot 1
props:
  type: object
  properties:
    label:
      title: Label
      type: string
    tag:
      title: Tag
      default: h5
      description: HTML tag, only letters and dash allowed.
      type: string
      pattern: '^[a-zA-Z0-9-]+$'
