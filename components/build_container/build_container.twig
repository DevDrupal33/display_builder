{#
/**
 * @file
 * Build Container component for the Display Builder.
 *
 * CSS isolation is handled through CSS containment properties
 * to prevent backend theme CSS from affecting builder content.
 */
#}
<div class="db-build-container" {{ attributes }}>
  {{- content -}}
</div>
