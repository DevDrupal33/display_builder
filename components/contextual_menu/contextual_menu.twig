<sl-menu{{ attributes }}>
  <div class="db-menu__header">
    <sl-menu-label class="db-menu__label">
      <span class="db-menu__name">{% if label %}{{ label }}{% endif %}</span>
      <span class="db-menu__path"></span>
    </sl-menu-label>
    {{ include('display_builder:icon_button', {
      icon: 'x-lg',
      label: 'Close'|t,
      attributes: {class: 'db-menu__close'},
    }, with_context: false) }}
  </div>
  {% for item in items %}
    {{ item }}
  {% endfor %}
</sl-menu>
