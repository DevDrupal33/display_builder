{% if variant and variant != 'default' %}
  {% set attributes = variant ? attributes.addClass('db-dropzone--' ~ variant) : attributes %}
{% endif %}
{% set attributes = attributes.setAttribute('tabIndex', 0) %}
<div{{ attributes.addClass('db-dropzone') }}>{{ content }}</div>
