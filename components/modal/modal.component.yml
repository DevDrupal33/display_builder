name: Modal
props:
  type: object
  properties:
    id:
      title: ID
      $ref: ui-patterns://identifier
    resizable:
      title: "Resizable?"
      description: "Is the modal resizable?"
      type: boolean
libraryOverrides:
  dependencies:
    - core/drupal
    - core/once
    - display_builder/local-storage-manager
