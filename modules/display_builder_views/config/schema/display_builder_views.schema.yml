views.display_extender.display_builder:
  type: views_display_extender
  mapping:
    profile:
      label: 'Display Builder profile'
      type: string
      # @todo error on test views.spec.ts when deleting. Can not reproduce
      # manually, skip for now.
      # constraints:
      #   ConfigExists:
      #     prefix: 'display_builder.profile.'
    sources:
      label: 'UI Patterns sources'
      type: sequence
      sequence:
        type: ignore
      # @todo current config fail, see how to fix.
      # sequence:
      # type: ui_patterns_source.[source_id]
  constraints:
    FullyValidatable: ~
